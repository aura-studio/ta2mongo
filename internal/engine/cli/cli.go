// Package cli is the placeholder for the command-line engine mode: one-shot,
// CLI-driven ingestion. Not yet implemented — reserved so the engine layer has
// a slot for each runtime mode (daemon, gateway, cli, api).
package cli
