# worker.min.yaml — `tango worker run` 最精简配置：只含 required 字段。
# 其余字段全部走默认值（详见同目录 worker.max.yaml 的逐字段说明）。
runtime:
  mongo:
    uri: mongodb://localhost:27017/tango   # [required]
tasks:
  instanceID: worker-001                   # [required]（也可用 --instanceID 覆盖）
